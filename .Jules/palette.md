## 2024-05-24 - Accessibility improvements on FilterModal\n**Learning:** Added `accessibilityRole="button"` and meaningful `accessibilityLabel` attributes to various `Pressable` components in the `FilterModal` to improve screen reader accessibility.\n**Action:** Always verify custom interactive components (like `Pressable` wrapping icons/text) have appropriate accessibility roles and labels to ensure a robust user experience across all modalities.
